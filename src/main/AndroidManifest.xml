<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.listerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainInterface.SchoolTask_Main"></activity>
        <activity android:name=".MainInterface.SchoolTask_Add" />
        <activity android:name=".MainInterface.HomeTask_Add" />
        <activity android:name=".MainInterface.HomeTask_Main" />
        <activity android:name=".MainInterface.Recent" />
        <activity android:name=".MainInterface.Profile" />
        <activity android:name=".MainInterface.Dashboard" />
        <activity android:name=".MainInterface.RegisterForm" />
        <activity android:name=".MainInterface.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>